{
  "extends": "@react-native/typescript-config/tsconfig.json",
  "compilerOptions": {
    "moduleResolution": "node",
    "target": "es5", // 필요에 따라 추가
    "lib": ["es6", "dom"], // 필요에 따라 추가
    // 다른 옵션...
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "types/**/*.d.ts"  // 추가한 타입 선언 경로
]
}